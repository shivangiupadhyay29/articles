import { getArticles, addArticles, getArticle, toggleFavorite,  toggleFollowing, deleteArticle, editArticle} from './article.actions';

export default {
    getArticles,
    addArticles,
    getArticle,
    toggleFavorite,
    toggleFollowing,
    deleteArticle,
    editArticle,
    
}