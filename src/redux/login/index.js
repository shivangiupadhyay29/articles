import { login, register, restore, setSettings, getSettings, logout, profile, user } from './login.action';

export default {
    login,
    register,
    restore,
    getSettings,
    setSettings,
    logout,
    profile,
    user
}